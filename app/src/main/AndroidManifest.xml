<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.syntaxeventlottery">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SyntaxEventLottery"
        tools:targetApi="31">

        <!-- Google Play Services Meta-Data -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- MainActivity Declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities Declaration with android:exported attribute -->
        <activity android:name=".UserHomeActivity" android:exported="true" />
        <activity android:name=".OrganizerActivity" android:exported="true" />
        <activity android:name=".OrganizerCreateEvent" android:exported="true" />
        <activity android:name=".EventDetailActivity" android:exported="true" />
        <activity android:name=".UserProfileActivity" android:exported="true" />
        <activity android:name=".EditUserProfileActivity" android:exported="true" />
        <activity android:name=".EditEventActivity" android:exported="true" />
        <activity android:name=".FacilityProfileActivity" android:exported="true" />
        <activity android:name=".AdminActivity" android:exported="true" />
        <activity android:name=".AdminEventsActivity" android:exported="true" />
        <activity android:name=".AdminUsersActivity" android:exported="true" />
        <activity android:name=".AdminUserDetailActivity" android:exported="true" />
        <activity android:name=".AdminEventDetailActivity" android:exported="true" />
        <activity android:name=".EventWaitingListActivity" android:exported="true" />
        <activity android:name=".NotificationCenterActivity" android:exported="true" />

        <!-- New Activity Declaration: CreateUserProfileActivity -->
        <activity
            android:name=".CreateUserProfileActivity"
            android:exported="true"
            android:label="User Profile" />
    </application>

</manifest>
